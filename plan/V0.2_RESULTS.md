# v0.2 Implementation Results

Date: 2026-08-18
Branch: `feat/v0.2`
Version: `0.2.0`

Evidence for `V0.2_SPEC.md`. Anything not verified is listed in §8 rather than
assumed.

---

## 1. Acceptance cases

Every case in `V0.2_SPEC.md` §8, with the test that locks it:

| # | Case | Status | Evidence |
|---|---|---|---|
| A | `if`/`else` becomes a condition, `d()` after it | Met | `JavaStructureExtractionTest` A, `V02AcceptanceTest` A |
| B | `if` without `else` has only a THEN section | Met | `JavaStructureExtractionTest` B |
| C | `switch` keeps case labels and default | Met | `JavaStructureExtractionTest` C, plus an arrow-switch case |
| D | Kotlin `when` has the same shape | Met | `KotlinStructureExtractionTest` D, plus a subjectless `when` |
| E | A repeated condition appears once inside the loop | Met | `JavaStructureExtractionTest` E, `V02AcceptanceTest` E |
| F | A for-each iterable is evaluated before the loop | Met | Java, Kotlin, and Go F cases |
| G | `do`-`while` records that the body runs at least once | Met | Java and Kotlin G cases |
| H | `try`/`catch`/`finally` sections with catch types | Met | Java and Kotlin H, `V02AcceptanceTest` H |
| I | `return` terminates after its expression | Met | Java, Kotlin, Go I cases |
| J | `throw` evaluates its expression first | Met | Java and Kotlin J cases |
| K | Go `switch` with an init statement | Met | `GoStructureExtractionTest` K |
| L | Go `select` is marked as a select | Met | `GoStructureExtractionTest` L |
| M | Structures nest | Met | Java, Kotlin, Go M cases, `StructureCanvasTest` |
| N | A call inside a branch is not marked conditional | Met | Java, Kotlin, Go N cases, `V02AcceptanceTest` N |
| O | Short-circuit operands keep the marker and the disclosure | Met | Java, Kotlin, Go O cases |
| P | Represented control flow drops the disclosure | Met | Java, Kotlin, Go P cases, `V02AcceptanceTest` P |
| Q | An empty branch stays visible | Met | `JavaStructureExtractionTest` Q, `StructureCanvasTest` |
| R | Budget exhausted inside a branch | Met | `V02AcceptanceTest` R, `FlowStructureBuilderTest` |

## 2. What v0.2 added on top of v0.1

- **Structure in the model.** `FlowNode` gained `branches` and
  `structureSummary`; a structure is one event in its parent's sequence that owns
  labelled sections. Frames stay flat, so nothing else in the engine changed
  shape. Structures consume the node budget but not callable depth.
- **A builder that can be interrupted mid-branch.** `openStructure` /
  `openBranch` / `closeStructure` route events into the innermost open section,
  and `closeOpenStructures` seals whatever is still open so a cancelled or
  truncated run keeps what it found inside a branch.
- **A tree instead of a call list from the analyzers.** `extractDirectFlow` now
  returns `FlowItem`s — calls, structures, terminators — for Java, Kotlin, and
  Go, in evaluation order per `V0.2_SPEC.md` §4.
- **Stacked-section rendering.** One container per structure, a labelled row per
  section, indented events, the sequence resuming below. Keyboard navigation
  walks into sections, so nothing is mouse-only.
- **A disclosure that means something.** `controlFlowIncomplete` is now raised
  only for control flow v0.2 does not model, so it points at a specific
  construct instead of appearing on every branch.

## 3. Test inventory

```text
core   unit tests            37
plugin fixture/integration  247
total                       284   all passing
```

New suites: `FlowStructureBuilderTest` (core), `JavaStructureExtractionTest`,
`KotlinStructureExtractionTest`, `GoStructureExtractionTest`,
`StructureCanvasTest`, and `V02AcceptanceTest`. `ConditionalCallTest` was
rewritten around the §5 change.

## 4. Plugin Verifier

Against **IU-261.27258.48** (minimum) and **IU-262.9437.185** (latest claimed),
run locally on 2026-08-18:

```text
Verdict                       Compatible (both)
Compatibility problems        0
Internal API usages           0
Scheduled-for-removal usages  0
Obsolete API usages           0
Deprecated API usages         0
Experimental API usages       0
```

Both reports contain a verdict and dependency listing only — no problem or
warning files were produced.

## 5. Defects found by testing

Two defects were found and fixed before the milestone closed. Both compiled and
passed the tests that existed at the time; both were caught by the new
per-language structure suites.

1. **A Kotlin subjectless `when` dropped its guards.** `walkWhen` walked each
   entry's body but never its conditions, so a call in a `when { cond() -> … }`
   guard vanished from the map entirely — the worst failure mode for this tool,
   since a missing call is invisible rather than wrong-looking. Found by
   `KotlinStructureExtractionTest`.
2. **A Go loop swallowed its header.** `walkLoop` walked every child into the
   body, putting the range expression and the `for` initializer inside the
   container. Both run once, before the loop; only the condition and the post
   statement repeat. Found by `GoStructureExtractionTest`.

Both are the same shape as the Go `getCondition()` defect from Milestone 0: a
structure's header and its body have to partition the children, and getting that
partition wrong either duplicates a call or loses one. The lesson carried
forward is that each language needs its own structure suite — a shared
Java-shaped test would have missed both.

## 6. Layout decision

`VISUAL_DESIGN.md` §13 originally showed a left/right fork. The owner chose
stacked labelled sections on 2026-08-18, and the section now records the
decision and the reason: a fork needs at least two card widths, the tool window
is routinely narrower than that, and a `switch` with several cases would be
unusable. Stacking meets every stated requirement at one card width and scales
to nesting.

## 7. Known limitations recorded

`KNOWN_LIMITATIONS.md` §25–§27 now carry the v0.2 boundaries: structure is not
reachability, the constructs still unrepresented (`switch` fall-through,
`break`/`continue` edges, catch-to-throw routing, short-circuit operands, Go
`select` runtime semantics), and branch labels being truncated source excerpts
that never reach logs.

## 8. Not verified in this milestone

- **Manual sandbox verification** of the new rendering. Per the owner's standing
  instruction, production-IDE checks happen only at v1.0; sandbox verification of
  v0.2 has not been performed at the time of writing.
- **Performance of deeply nested structures.** The benchmark corpus is call-heavy
  rather than structure-heavy, so layout cost for a body with many nested
  containers is not measured.
- **Reachability, fall-through, and exception routing**, which are out of scope
  by `V0.2_SPEC.md` §2 and recorded as limitations rather than gaps.
