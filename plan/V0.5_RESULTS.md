# v0.5 Implementation Results

Date: 2026-08-19 (hardening: same day, after external review)
Branch: `feat/v0.5`, then `fix/v0.5-hardening`
Version: `0.5.0`

Evidence for `V0.5_SPEC.md`. Anything not verified is listed in §7 rather than
assumed.

---

## 1. Acceptance cases

| # | Case | Status | Evidence |
|---|---|---|---|
| A | `executor.submit(() -> charge())` | Met | `JavaCallbackTest` A, `V05AcceptanceTest` A |
| B | Kotlin `launch { … }` | Met | `KotlinCallbackTest` B |
| C | Kotlin `inline` receiver | Met | `KotlinCallbackTest` C, plus the `noinline`, `crossinline`, mixed, and named-argument counter-cases |
| D | Kotlin `withContext(io) { … }` | Met | `KotlinCallbackTest` D |
| E | A call to a `suspend fun` | Met | `KotlinCallbackTest` E |
| F | Java `list.forEach(x -> …)` | Met | `JavaCallbackTest` F |
| G | A lambda given to an unlisted method | Met | `JavaCallbackTest` G, `V05AcceptanceTest` Q |
| H | Go `go func() { … }()` | Met | `GoCallbackTest` H (timing, not walked in place, body enterable, and not propagated to arguments) |
| I | Go `defer func() { … }()` | Met | `GoCallbackTest` I |
| J | Two lambdas in one call | Met | `JavaCallbackTest` J, `V05AcceptanceTest` J (one call, two bodies, distinguishable) |
| K | A body at the depth limit | Met | `V05AcceptanceTest` K |
| L | A body exhausting the node budget | Met | `V05AcceptanceTest` L |
| M | A body calling its enclosing callable | Met | `V05AcceptanceTest` M |
| N | A lambda never passed to a call | Met | `JavaCallbackTest` N, `KotlinCallbackTest` N, `GoCallbackTest` N, `V05AcceptanceTest` N |
| O | The step after a call with a callback | Met | `V05AcceptanceTest` O (asserts what precedes `save()`), `ExportTest` topology |
| P | All callbacks justified | Met | `V05AcceptanceTest` P |
| Q | An unknown-timing callback | Met | `V05AcceptanceTest` Q (counted, and the count selects the card) |
| R | Export with callbacks | Met | `ExportTest` R (both formats, both async and undetermined) |
| S | A nested callback | Met | `KotlinCallbackTest` S |
| T | A branch inside a callback body | Met | `V05AcceptanceTest` T |

## 2. What v0.5 added

- **`FlowNodeKind.CALLBACK`**, a body handed to a call. It owns a frame the way
  a call does — that is what makes the body enterable — but has no dispatch,
  because there is nothing to choose between.
- **A timing per callback**, carried on the two dimensions that already existed:
  `ExecutionMode` for *how* it runs and `OrderingStatus` for *whether the
  position on the canvas means anything*. No new visual grammar was needed; the
  dashed connector that v0.1 introduced for conditional calls already says it.
- **Three justifications, and no fourth.** Kotlin's `inline` rule (a language
  guarantee), Go's `go`/`defer` keywords, and a hand-written list of documented
  APIs. Everything else is `UNKNOWN`, said out loud.
- **`isAnonymousBody`**, which keeps a lambda out of the dispatch chooser and
  the saved-flow store. A lambda is analyzable as a frame but is not something a
  reader can name or store: its identity is a position in a file.

## 3. Test inventory

```text
core   unit tests             63
plugin fixture/integration   388
total                        451   all passing

Also green with -Pflowlens.isolate=true, one JVM per test class.
```

New suites: `JavaCallbackTest`, `KotlinCallbackTest`, `GoCallbackTest`,
`V05AcceptanceTest`.

## 4. Plugin Verifier

Against **IU-261.27258.48** and **IU-262.9437.185**, run locally on 2026-08-19:

```text
Verdict                       Compatible (both)
Compatibility problems        0
Internal API usages           0
Scheduled-for-removal usages  0
Obsolete API usages           0
Deprecated API usages         0
Experimental API usages       0
```

## 5. Defects found before the milestone closed

1. **A callback could not own the body it was supposed to show.** `FlowNode`
   required a frame-owning event to be a call, a constructor, or the entry, so
   the first end-to-end run threw `IllegalArgumentException` the moment a
   callback opened its child frame — and the run reported `FAILED` with the
   frame half-built. Every unit test passed, because extraction never builds a
   model.

   The fix is the invariant saying what it means: a callback owns a frame for
   the same reason a call does — there is a body to enter. What differs is when
   it runs, not whether it has one.

   The lesson is the one v0.4 kept teaching in a different costume: a model that
   is correct at one layer says nothing about the layer that consumes it. The
   acceptance suite exists to cross that boundary, and it is what caught this.

2. **A lambda became a dispatch candidate.** Making a lambda an analyzable
   declaration is what lets the run enter its body — but `DefinitionsScopedSearch`
   returns lambdas as implementations of a functional interface method, so the
   Choose Implementation dialog would have started offering entries named `{ }`
   whose stored key is a file offset that does not survive an edit.

   `isAnonymousBody` now separates the two questions: *can this be analyzed as a
   frame* and *is this something a reader can name and store*. Pins were already
   safe by construction — a pin is refused unless its key round-trips — which is
   worth recording as a guardrail that paid for itself.

3. **An immediately invoked Go closure was named by its whole source text.**
   `calleeNameOf` fell back to `expression.text`, so `func() { charge() }()`
   produced a card titled with the entire literal. It is `func()` now. This one
   predates v0.5 but only became visible when closures stopped being skipped.

4. **Java disagreed with the other two analyzers about a lambda's body.**
   `hasAnalyzableBody` still answered only for methods, so Java said a lambda has
   no body it can traverse while Kotlin and Go said it has. Nothing depended on
   it — the run does not consult it for callbacks — which is exactly why it would
   have sat there until something did.

5. **`enum.execution.UNKNOWN` read as "Unknown"**, which in a tooltip sitting
   next to a resolution status says nothing about *what* is unknown. It is
   "Timing not determined" / 「実行タイミング不明」 now — the point of the state is
   that the reader knows which question went unanswered.

6. **The first call in a frame never showed a project boundary** — found in the
   sandbox, and older than v0.5. The marker is drawn on the connector between
   two cards, and the first card has no predecessor, so `place()`'s two JDK calls
   were drawn differently: `Iterable.forEach()` was marked as crossing out of the
   project and `ExecutorService.submit()`, two cards above it, was not.

   The view model had it right all along — `boundaryBeforeCard` was already true
   for both — and the existing test asserted exactly that. Only the layout and
   the painting ignored it, which is the same shape as defect 2 in `V0.4_RESULTS.md`:
   a rule implemented everywhere except where the reader looks.

7. **A Go closure invoked in place produced two cards, one of them a failure.**
   Found in the sandbox. `go func() { charge() }()` drew an unresolved call card
   — `? ⚡ func()()`, dashed and red — directly above the callback card that had
   the body. There is no declaration to resolve here: the call and the body are
   the same thing, so the call card could only ever report itself as a dead end,
   and the doubled parentheses came from a short name that already ended in one.

   An invoked literal is now one event. A literal *handed* to a call still gets
   both, because there the call and the body really are two things.

8. **A gap left open on purpose**, recorded rather than quietly shipped: a
   method reference (`list.forEach(this::audit)`) and an anonymous class or
   object literal are still not callback events, so the same loop shows its body
   when written as a lambda and shows nothing when written as a reference
   (`KNOWN_LIMITATIONS.md` §45). The reason is scope, not difficulty.

## 6. v0.5 hardening — found by external review

The review was right on every count, and the first finding is the one that
mattered: **an asynchronous body was drawn as if the flow ran through it.**

1. **A callback was a link in the chain.** The model listed it as a sibling
   event, and every surface read the sequence off adjacency: the canvas drew a
   solid connector from the callback to `save()`, and the Mermaid walk advanced
   its frontier to the callback, so the edge into `save()` came from the body.
   For an asynchronous body that says the opposite of what is known — "once the
   body has run, `save()`" — when the whole point is that nobody knows when it
   runs.

   `V0.5_SPEC.md` §5.5 said `save()` follows `submit()`. The implementation said
   otherwise on two surfaces, and acceptance case O could not tell, because it
   asserted the *connector style* and never asked what precedes `save()`.

   Fixed as a **model relation**: a callback node names the call it is attached
   to (`FlowNode.attachedTo`), and every surface derives the connection from
   that. The canvas sets an attached body in from the left and runs the sequence
   connector down the gutter past it; Mermaid draws the edge from the call and
   keeps its frontier there; Markdown writes the body under the call rather than
   beside it. A drawing convention would have left the three free to disagree
   again.

2. **The Java list claimed timings the API does not promise.** Three entries
   were removed after checking the contracts rather than the folklore:
   `Stream.map`/`filter`/`peek` are lazy intermediate operations and may never
   run; `Executor.execute` "may execute in a new thread, in a pooled thread, or
   in the calling thread"; `new Thread(runnable)` starts nothing at all.
   `ExecutorService.submit` and `CompletableFuture.*Async` stay, because their
   contracts do say the work is asynchronous.

   This is the failure mode the list exists to avoid, committed inside the list
   itself: a plausible entry is worse than a missing one, because it is trusted.

3. **Kotlin `crossinline` was read as "runs in place".** The rule checked only
   `noinline`. `crossinline` keeps a lambda inlined but permits it to be invoked
   from another execution context — a local object, a nested function, another
   thread — which is exactly the case the `inline` guarantee does not cover.

   The same fix made the rule per-parameter instead of per-call: `inline fun
   f(a: () -> Unit, noinline b: () -> Unit)` makes a promise about `a` that it
   does not make about `b`, and the old all-or-nothing check threw away the half
   that could be justified. Named arguments are matched by name.

4. **Go's keywords were applied to arguments.** `go helper(func() { … })` marked
   the literal as a goroutine, but the Go specification is explicit that "the
   function value and parameters are evaluated as usual in the calling
   goroutine". What `helper` does with the literal decides when it runs, which
   is `UNKNOWN`. The keyword still applies to a literal that is itself invoked.

5. **Acceptance J tested something else.** The case is two lambdas in *one*
   call; the test chained two calls with one lambda each, which looked the same
   and exercised nothing. The real case then exposed a second defect: both
   callbacks of one call had the same display name *and the same symbol key*, so
   they were indistinguishable to the reader and to anything keyed by symbol.
   They are now `#1` and `#2` by argument position, which is an identity and not
   an order (`KNOWN_LIMITATIONS.md` §44).

6. **`callsInPlace` was in the spec and not in the code.** Not a defect — the
   unread contract can only move an answer from `UNKNOWN` towards `SYNC`, so it
   errs safely — but the spec claimed something the implementation did not do.
   The spec now says deferred, and §46 records it.

The lesson is narrower than the earlier ones and worth keeping separate: **a
test that asserts how something is drawn is not a test of what it means.** Case
O checked that the callback's connector was dashed and `save()`'s was solid.
Both were true, and the map was still wrong, because the question that decides
the meaning — *what is `save()` connected to* — was never asked.

## 7. Not verified here

- **Production IDE.** The ZIP has not been installed into a real IntelliJ IDEA
  Ultimate. That is v1.0's job (`PLAN.md` §17), and the sandbox plus the Verifier
  are what stand in for it until then.
- **Real projects.** Timing coverage was measured against the fixtures and the
  manual samples, not against a codebase that did not know it was being read.
  How often `UNKNOWN` appears in practice — and therefore whether the documented
  list is the right size — is an open question for the stabilization milestone.
- **The Japanese pass.** New strings were translated as they were added, but the
  full read-through of the Japanese bundle in context is v1.0 work.
